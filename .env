# Copy this file to .env and fill in real values before running the app.

# SQLite for local dev — this creates a file-based database, zero setup required.
DATABASE_URL="file:./dev.db"

# Any long random string — used to sign login sessions (JWTs).
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="0f8bea62a3b835d932f049457afc7d2d73c24379b9fdc1d9db15f4f1059e9e0fd535afd2646c881dc6264727d458d84d"
